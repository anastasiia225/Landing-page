
/*animation*/
/*header---------------------------------------*/
/*$(window).scroll(function() {
    $('.animated_logo').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      if (imagePos < topOfWindow) {
        $(this).addClass('zoomIn');
      }
    });
  });*/
/*2--------------------------------------------*/
$(window).scroll(function() {
    $('.block2_img1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block2_img2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block2_img3').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block2_img4').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block2_text-img').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInUpBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block2_text-img1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
/*3------------------------------------------------------------*/
$(window).scroll(function() {
    $('.block_3_content_head1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block_3_content_head3').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.hotel1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInUpBig');
      }
    });
  });
$(window).scroll(function() {
    $('.hotel2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInUpBig');
      }
    });
  });
$(window).scroll(function() {
    $('.hotel3').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInUpBig');
      }
    });
  });
$(window).scroll(function() {
    $('.hotel4').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInUpBig');
      }
    });
  });
/*$(window).scroll(function() {
    $('.hotel1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('bounceInUp');
      }
    });
  });
$(window).scroll(function() {
    $('.hotel2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('bounceInLeft');
      }
    });
  });
$(window).scroll(function() {
    $('.hotel3').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('bounceInUp');
      }
    });
  });
$(window).scroll(function() {
    $('.hotel4').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('bounceInUp');
      }
    });
  });*/
$(window).scroll(function() {
    $('.block_3_content_head2 ').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('zoomIn');
      }
    });
  });
/*4------------------------------------------------------------*/
$(window).scroll(function() {
    $('.block4_background').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('zoomIn');
      }
    });
  });
$(window).scroll(function() {
    $('.block4_head1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block4_head2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block4_content_1of21').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block4_content_1of23').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height)  {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block4_img ').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height+300) {
        $(this).addClass('zoomIn');
      }
    });
  });
$(window).scroll(function() {
    $('.block4_content_1of22 ').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block4_content_1of24 ').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block4_content_1of25 ').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.content_head').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadenLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block5_content2_head').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block5_animate_content').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height+50) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block5_animate_in_right').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block5_content2_foto2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block5_content41').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block5_content42').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block5_content2_foto21').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.button_block5_align').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('zoomIn');
      }
    });
  });
$(window).scroll(function() {
    $('.block6_head1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block6_head2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block6_content1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block6_content2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block6_content3').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block6_content4').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block6_content5').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
/*7------------------------------------------------------------*/
$(window).scroll(function() {
    $('.block7_head1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block7_head2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
  $(window).scroll(function() {
    $('.block7_img1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block7_img1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block7_img2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block7_img3').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block7_img4').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
$(window).scroll(function() {
    $('.block7_img5').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
/*8------------------------------------------------------------*/
$(window).scroll(function() {
    $('.block8_head1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeftBig');
      }
    });
  });
  $(window).scroll(function() {
    $('.block8_head2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
  $(window).scroll(function() {
    $('.block8_head3').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRightBig');
      }
    });
  });
    $(window).scroll(function() {
    $('.block8_img111').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeIn');
      }
    });
  });
        $(window).scroll(function() {
    $('.block8_img222').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeIn');
      }
    });
  });
            $(window).scroll(function() {
    $('.block8_img333').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeIn');
      }
    });
  });
  $(window).scroll(function() {
    $('.block8_button').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeIn');
      }
    });
  });
  /*9----------------------------------------------------------*/
    $(window).scroll(function() {
    $('.block9_content1').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInLeft');
      }
    });
  });
        $(window).scroll(function() {
    $('.block9_content2').each(function(){
      var imagePos = $(this).offset().top;
      var topOfWindow = $(window).scrollTop();
      var height = $(window).height();
      if (imagePos < topOfWindow+height) {
        $(this).addClass('fadeInRight');
      }
    });
  });
/*banner-------------------------------------------------------*/
$(document).ready(
    function(){
        $(window).resize(function(){
            var height = $(window).height();
            $('.banner_img').css({
                'height':height,
                'width':'100%',
                'opacity':'0',
                'transition':'16s'
            });
        }).resize();
    }
);
$(document).ready(
    function blyat(){
        $(window).resize(function(){
            var height = $(window).height();
            $('.banner_img2').css({
                'height':height,
                'width':'100%',
                'opacity':'0',
                'transition':'32s'
            });
        }).resize();
    }
);
$(document).ready(
    function(){
        $(window).resize(function(){
            var height = $(window).height();
            $('.banner_img').css({
                'height':height,
                'width':'100%',
                'opacity':'0',
                'transition':'32s'
            });
        }).resize();
    }
);
$(document).ready(
    function (){
        $(window).resize(function(){
            var height = $(window).height();
            $('.banner_img2').css({
                'height':height,
                'width':'100%',
                'opacity':'0',
                'transition':'4s'
            });
        }).resize();
    }
);
$(document).ready(
    function(){
        $(window).resize(function(){
            var height = $(window).height();
            $('.banner').css({
                'height':height,
                'width':'100%'
            });
        }).resize();
    }
);
$(document).ready(
    function(){
        $(window).resize(function(){
            var width = $(window).width();
            $('navigation').css({
                'width':width
            });
        }).resize();
    }
);
$(document).ready(
    function(){
        $(window).resize(function(){
            var height = $(window).height();
            $('.banner_img1').css({
                'height':height,
                'width':'100%'
/*                'opacity':'1',
                'transition':'5s'*/
            });
        }).resize();
    }
);
$('.block7_img11_hide').hide();

        $('.block7_img1').on('hover', function(){
            $('.block7_img11_hide').fadeIn(500);
        });

/*scroll---------------------------------------------------*/
$(window).on('scroll', function(){
	var height = $(window).height();
	if($(window).scrollTop() >= 5){
		$("html,body").animate({scrollBy:height}, 300)
	}
});
/*hide-----------------------------------------------------*/
$('.block8_hide').hide();
$('.block8_button').on('click', function(){
	$(this).hide();
	$('.block8_hide').slideToggle(500);
});
/*slider---------------------------------------------------*/
/*$(document).ready(function(){
  $('.next').click(function(){
    var currentImage = $('.img.curry');
    var currentImageIndex = $('.img.curry').index();
    var nextImageIndex = currentImageIndex + 1;
    var nextImage = $('.img').eq(nextImageIndex);
    currentImage.fadeOut(0);
    currentImage.removeClass('curry');

    if(nextImageIndex == ($('.img:last').index()+1)){
      $('.img').eq(0).fadeIn(1500);
      $('.img').eq(0).addClass('curry');
    }else{
      nextImage.fadeIn(1500);
      nextImage.addClass('curry');
    }
  });
  $('.prev').click(function(){
    var currentImage = $('.img.curry');
    var currentImageIndex = $('.img.curry').index();
    var prevImageIndex = currentImageIndex - 1;
    var prevImage = $('.img').eq(prevImageIndex);

    currentImage.fadeOut(0);
    currentImage.removeClass('curry');
    prevImage.fadeIn(1500);
    prevImage.addClass('curry');

  });
});*/
/*$(function(){
  $('.sl').slick({
    autoplay:true,
    slidesToShow:5,
    dots:true
  });
});*/
$(function(){
  $('.sl2').slick({
    autoplay:true,
    slidesToShow:4,
    dots:true,
    slidesToScroll:4,
    arrows:false,
    responsive:
    [
      {
      breakpoint:992,
      settings:
        {
          slidesToShow:3,
          slidesToScroll:3
        }
      },
      {
        breakpoint:767,
      settings:
        {
          slidesToShow:2,
          slidesToScroll:2
        }
      },
      {
        breakpoint:560,
      settings:
        {
          slidesToShow:1,
          slidesToScroll:1
        }
      }
    ]
  });
});
/*scroll---------------------------------------*/
/*var anchors = [];
var currentAnchor = -1;
var isAnimating  = false;
var width1 = $(window).width();
if(width1>=1170){  $(function(){
      
      function updateAnchors() {
          anchors = [];
          $('.anchor').each(function(i, element){
              anchors.push( $(element).offset().top );
          });
      }
      
      $('body').on('mousewheel', function(e){
          e.preventDefault();
          e.stopPropagation();
          if( isAnimating ) {
              return false;
          }
          isAnimating  = true;
          // Increase or reset current anchor
          if( e.originalEvent.wheelDelta >= 0 ) {
              currentAnchor--;
          }else{
              currentAnchor++;
          }
          if( currentAnchor > (anchors.length - 1) 
             || currentAnchor < 0 ) {
              currentAnchor = 0;
          }
          isAnimating  = true;
          $('html, body').animate({
              scrollTop: parseInt( anchors[currentAnchor] )
          }, 1000, 'swing', function(){
              isAnimating  = false;
          });
      });

      updateAnchors();   
      return false;
  });}*/
/*scroll off media----------------------------*/
/*function(){
  var width = $(window).width();
  if(width<=1170){
    $('banner').removeClass('anchor');
  }
}
function(){
  var width = $(window).width();
  if(width<=1170){
    $('block2').removeClass('anchor');
  }
}
function(){
  var width = $(window).width();
  if(width<=1170){
    $('block4').removeClass('anchor');
  }
}
function(){
  var width = $(window).width();
  if(width<=1170){
    $('block5').removeClass('anchor');
  }
}
function(){
  var width = $(window).width();
  if(width<=1170){
    $('block5_content4').removeClass('anchor');
  }
}
function(){
  var width = $(window).width();
  if(width<=1170){
    $('block5_content2_foto2').removeClass('anchor');
  }
}
function(){
  var width = $(window).width();
  if(width<=1170){
    $('block6').removeClass('anchor');
  }
}
function(){
  var width = $(window).width();
  if(width<=1170){
    $('block7').removeClass('anchor');
  }
}
function(){
  var width = $(window).width();
  if(width<=1170){
    $('block8_background').removeClass('anchor');
  }
}
function(){
  var width = $(window).width();
  if(width<=1170){
    $('block9').removeClass('anchor');
  }
}*/